Drop your track in this folder with the exact filename:

  friendship.mp3

The floating music player at the bottom-right of the page will then
let Danish press play manually (it never autoplays). If the file is
missing, the player politely disables itself instead of throwing an
error.
